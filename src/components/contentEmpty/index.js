var Abstract = require('../Abstract');

module.exports = Abstract.extend({
    template: require('./empty.html'),

    components: {

    },

    ready: function () {
        var that = this;
    }
});
